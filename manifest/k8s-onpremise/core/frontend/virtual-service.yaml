apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: frontend
spec:
  hosts:
  - "marketplace.local"
  gateways:
  - frontend-gateway
  http:
  - name:
    route:
    - destination:
        host: frontend-dashboard.namespace.svc.cluster.local
